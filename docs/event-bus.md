# Custom window events

> **Read this when** dispatching or listening for custom window events in apps/desktop. **Not for** events between app windows, which go through Tauri `emit`/`listen` (see Cross-window events below).

Every event on this page is a browser CustomEvent sent via `window.dispatchEvent()` and received via `window.addEventListener()` or a picker's `openEvent` prop. Payloads are the `detail` field. Window events are the cross-feature entry: a feature asks the app shell to open a studio or a surface without importing it.

## Navigation

| Event                             | Payload | Dispatched from                                                                                                                                                                                                            | Listened in                                                                          | Purpose                                                 |
| --------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| `goodboy:new-session`             | none    | requestNewSession.ts (useAppShortcuts/index.ts, CommandPalette/index.tsx), OnboardingCard/StepRow.tsx, CollapsedRail.tsx, StageBoard/index.tsx, SessionActivityBar/index.tsx                                               | useSessionSurfaceEvents.ts, SessionActivityBar/index.tsx, NewSessionBridge/index.tsx | Launch an empty session on the current workspace.       |
| `goodboy:add-workspace`           | none    | OnboardingCard/StepRow.tsx, WorkspaceLauncher/index.tsx, WorkspaceSwitcher/index.tsx, CommandPalette/index.tsx                                                                                                             | useStudioEvents.ts                                                                   | Open the add workspace studio.                          |
| `goodboy:reveal-chat`             | none    | ChatBreadcrumb, HandoffChip, NotificationToastBridge, useBoardNavigation, useAgentsSection, useStandaloneAgentsLane, CreateAgentPopover, AgentFollowUpChild, ExploreSpawnPopover, useReportRegenerate, useWireframeRespawn | useSessionSurfaceEvents.ts, AgentDetailPane/index.tsx                                | Close the session studio and show the agent transcript. |
| `goodboy:open-workspace-switcher` | none    | useAppShortcuts/index.ts, WorkspaceLinkForm/index.tsx                                                                                                                                                                      | WorkspaceIdentityRow/index.tsx (via openEvent prop)                                  | Open the workspace switcher popover.                    |
| `goodboy:open-command-palette`    | none    | OnboardingCard/StepRow.tsx                                                                                                                                                                                                 | useStudioEvents.ts                                                                   | Open the command palette.                               |

## Studios

| Event                               | Payload                                                                                                                                                                 | Dispatched from                                                                                                                                                                                                                                                | Listened in                  | Purpose                                                                                                     |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `goodboy:open-settings`             | `{ scope?: 'app' \| 'workspace' \| 'providers' \| 'tools', section?: string, tool?: string, provider?/providerId?: string, action?: 'install' \| 'login' \| 'logout' }` | openToolSettings.ts, MountProjectAction.tsx, StageBoard/index.tsx, WorkspaceIdentityRow/index.tsx, WorkspaceSwitcher/index.tsx, NotificationToastBridge/index.tsx, ProviderGrid.tsx, NoConnectedProviders.tsx, NoProvidersNotice.tsx, CommandPalette/index.tsx | useStudioEvents.ts           | Open the settings studio at a scope, and optionally a section, tool or provider. Scope falls back to `app`. |
| `goodboy:open-guide`                | none                                                                                                                                                                    | SettingsStudio/AppScopePanel.tsx, CommandPalette/index.tsx                                                                                                                                                                                                     | useStudioEvents.ts           | Open the getting started studio.                                                                            |
| `goodboy:open-report-issue`         | none                                                                                                                                                                    | ReportIssuePopover/index.tsx, SettingsStudio/AppScopePanel.tsx, CommandPalette/index.tsx, sendNotificationToDevelopers.ts                                                                                                                                      | useStudioEvents.ts           | Open the bug report studio.                                                                                 |
| `goodboy:open-notifications-studio` | none                                                                                                                                                                    | NotificationCenter/index.tsx, CommandPalette/index.tsx                                                                                                                                                                                                         | useStudioEvents.ts           | Open the notifications studio.                                                                              |
| `goodboy:open-notifications`        | none                                                                                                                                                                    | Toast/index.tsx                                                                                                                                                                                                                                                | NotificationCenter/index.tsx | Open the notifications popover.                                                                             |
| `goodboy:open-pair-device`          | none                                                                                                                                                                    | SettingsStudio/AppScopePanel.tsx, CommandPalette/index.tsx                                                                                                                                                                                                     | useStudioEvents.ts           | Open the companion pairing studio.                                                                          |
| `goodboy:open-impact-studio`        | `{ scope?: object }`                                                                                                                                                    | openImpactStudio.ts (SessionCostChip.tsx, OrchestratorPanel/index.tsx, NotificationToastBridge/index.tsx, CommandPalette/index.tsx)                                                                                                                            | useStudioEvents.ts           | Open the impact studio, at a spend scope when one is given.                                                 |
| `goodboy:open-inbox`                | `{ workspaceId?, provider?, kind?, recordKey?, sessionId? }`                                                                                                            | TrackerStudioLinks/index.tsx, ToolDetailPanel.tsx, StageBoardCard/index.tsx, session-view/workSurface.ts, CommandPalette/index.tsx                                                                                                                             | useStudioEvents.ts           | Open the inbox studio, optionally scoped to a provider, kind, record or session.                            |
| `goodboy:open-workflow-studio`      | none                                                                                                                                                                    | WorkflowFollowToastBridge/index.tsx, CommandPalette/index.tsx                                                                                                                                                                                                  | useStudioEvents.ts           | Open the workspace workflow studio.                                                                         |
| `goodboy:open-changelog`            | none                                                                                                                                                                    | changelogStudioEvent.ts (CommandPalette/index.tsx)                                                                                                                                                                                                             | useStudioEvents.ts           | Open the changelog studio.                                                                                  |

## Session

| Event                           | Payload                  | Dispatched from                                                                                                                | Listened in                | Purpose                                                   |
| ------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------- | --------------------------------------------------------- |
| `goodboy:open-gitlab-mr`        | `{ sessionId }`          | GitlabMrStrip.tsx, PrPane.tsx                                                                                                  | useSessionSurfaceEvents.ts | Open the GitLab MR session studio.                        |
| `goodboy:open-bitbucket-pr`     | `{ sessionId }`          | BitbucketPrStrip.tsx, PrPane.tsx                                                                                               | useSessionSurfaceEvents.ts | Open the Bitbucket PR session studio.                     |
| `goodboy:open-plan-studio`      | `{ sessionId, planId? }` | PlanChip/index.tsx, WorkflowStepPlanBadge.tsx, AgentBriefPlans.tsx                                                             | useSessionSurfaceEvents.ts | Open the plans lens, focused on a plan when one is given. |
| `goodboy:open-workflow-builder` | `{ sessionId }`          | ChatEmptyState.tsx, commandExecutor.ts, WorkflowAttachButton/index.tsx, WorkflowStartButton.tsx, SessionOverviewPane/index.tsx | useSessionSurfaceEvents.ts | Open the workflow builder session studio.                 |

## Pickers and progress

| Event                              | Payload                             | Dispatched from          | Listened in                                                  | Purpose                                   |
| ---------------------------------- | ----------------------------------- | ------------------------ | ------------------------------------------------------------ | ----------------------------------------- |
| `goodboy:open-model-picker`        | none                                | useAppShortcuts/index.ts | ChatInput/index.tsx (via openEvent prop)                     | Open the model picker.                    |
| `goodboy:open-permission-picker`   | none                                | useAppShortcuts/index.ts | PermissionModePicker/index.tsx (via openEvent prop)          | Open the permission mode picker.          |
| `goodboy:workflow-step-started`    | `{ stepId, stepIndex, stepLabel? }` | activateWorkflowAgent.ts | WorkflowFollowToastBridge/index.tsx                          | Announce that a workflow step began.      |
| `goodboy:onboarding-progress`      | none                                | onboarding-store.ts      | useOnboardingWizard/index.ts, useOnboardingProgress/index.ts | Signal an onboarding step completion.     |
| `goodboy:open-onboarding-wizard`   | `{ mode?: string }`                 | onboarding-store.ts      | useOnboardingWizard/index.ts                                 | Open the onboarding wizard at a mode.     |
| `goodboy:cursor-max-mode-advisory` | none                                | cursorMaxModeAdvisory.ts | cursorMaxModeAdvisory.ts (subscribe)                         | Signal a Cursor Max Mode advisory update. |

## Mock scenes only

`goodboy:mock-open-cursor-picker` and `goodboy:mock-open-codex-picker` are `openEvent` names that `ModelPickerScenes.tsx` hands to the model picker so a mock scene can open it. Nothing in the app dispatches them, on purpose.

## Cross-window events

Events between app windows use Tauri `emit`/`listen`, not the window bus. `features/workspace/window.ts` owns `goodboy:presence`, `goodboy:presence-request` and `goodboy:window-closing` (which workspace each window shows). `features/review/prWriteBus.ts` owns `goodboy:pr-write`, which claims and releases a pull request write so two windows never push the same PR at once.

## Adding an event

Event names follow the pattern `goodboy:verb-noun` (e.g., `goodboy:open-plan-studio`, `goodboy:reveal-chat`). Every event must be dispatched and listened in the same pull request, and its row lands here in the same commit; a listener without a dispatch is unreachable code. Payloads should stay minimal: pass only the ids and enums needed to route or initialize a feature, never entire objects. If an event grows past three fields, reconsider whether routing logic belongs in a store method instead.

import type { ProviderId } from '@goodboy/types';

export const CLI_LABEL: Record<ProviderId, string> = {
  anthropic: 'Claude CLI',
  cursor: 'Cursor CLI',
  codex: 'Codex CLI',
  gemini: 'Gemini CLI',
  opencode: 'OpenCode CLI',
  openrouter: 'OpenCode CLI',
  moonshot: 'OpenCode CLI',
};
